# 科学假设 Agent 改进后结果

## 结论

最终有效运行逐字复用了改进前冻结的提示和评分规程。主 Agent 调用了
`solar-hypothesis`，子 Agent 完成请求绑定、上游检查、证据绑定、响应验证、
七维排序和冻结保存。正式产物通过合同回读、语义复查和哈希复算。

冻结量表得分由基线的 **12/16** 提升到 **16/16**。

## 运行身份与证据边界

| 项目 | 记录 |
| --- | --- |
| 冻结评测起始主线 | `37a2088dbadc220c66326d00bf0cfad3ef82faec` |
| 最终同步主线 | `007a084540eb562fa267e894eb3258d62b5eb7f1` |
| 核心集成提交 | `d7f5de9` |
| 真实运行加固提交 | `b1f5202` |
| 前端 | `http://localhost:4716`，HTTP 200 |
| 后端 | `http://127.0.0.1:6174`，`/ok` 返回 `{"ok":true}` |
| provider | `deepseek` |
| model | `deepseek-v4-pro` |
| 冻结提示 | `docs/evals/hypothesis-frontend/2026-07-26-baseline-prompt.md` |
| 评分规程 | `docs/evals/hypothesis-frontend/2026-07-26-rubric.md` |
| 最终 thread | `019f9d16-70cd-7460-ab41-be770bfee5fd` |
| 最终 run | `019f9d16-70d1-7b63-88ee-371db3cad93d`，`success` |
| 科学假设产物 run_id | `experiment_agent_handoff_demo-20260726T062812Z-26d4b2d0` |

前后端均由改进后的工作树真实启动，健康检查通过。当前 Codex 任务以 WSL URI
打开，Windows 侧浏览器控制器在进入页面前拒绝该路径；本地也没有可复用的
Playwright/Chromium。因此，最终提示通过同一 WebUI 后端的 LangGraph SDK
提交，不能替代浏览器视觉检查。前端生产构建和 HTTP 页面检查已通过；仍建议在
Windows 原生路径打开的新任务中补一次人工视觉确认。

## 最终运行轨迹

1. 主 Agent 读取系统技能和记忆后，调用 `task → solar-hypothesis`；没有创建、
   复制或手工补入 `hypothesis/inputs/`，也没有直接调用
   `scientific_hypothesis_*` 工具代写。
2. 子 Agent 返回的合同回执记录了以下闭合顺序：
   `bind_request → inspect_upstream → bind_evidence → validate_response → rank → freeze`。
3. 两个实验 run 路径均不存在。已完成复算记录、技术失败的极区磁场实验和未经
   原文核验的文献笔记均按未核验材料处理，绑定为 `gap`；技术失败没有被当作
   反对证据。
4. 响应共提交三轮，前两轮合计发现八项无依据定量表述，修订后通过。正式候选
   保留请求中已有的“约 8 个月”背景，不再自行分配未经证据支持的月份贡献。
5. 七维排序覆盖四个候选，连续名次为 1、2、3、4。因没有任何已核验支持证据，
   四个候选置信度均为 `low`，关键证据锚点为空，没有虚构锚点。
6. `freeze` 首次成功，写入三份正式文件；本次没有执行新实验。

父任务状态只保留 `task` 调用和子 Agent 的最终合同回执，不暴露子任务内部的
逐条原始工具消息。因此，工具顺序以回执记录，冻结状态、路径、排序和内容则由
落盘文件独立复核。

## 开发中发现并修复的问题

| 运行 | 结果 | 对应修复 |
| --- | --- | --- |
| `019f9cbd-c7d9-7b53-8ae7-264c3f29397b` | 主 Agent 请求创建 `/hypothesis/inputs`，触发审批中断 | 明确 bundled input 必须原样传给子 Agent，主 Agent 不得探测或暂存 |
| `019f9cd7-3729-7230-ae0d-50a5733de2bd` | 排序通过，但冻结报缺少 `rubric` | 缓存通过预检的原始排序请求，不把去掉 `rubric` 的组合形状重新送入请求校验 |
| `019f9ce4-0b0c-75a2-a78c-a4f20529e6f8` | 冻结成功，但产物含无依据的月份贡献拆分 | 把数值可追溯门禁扩展到候选主张和机制 |
| `019f9cf1-4e20-7b53-8b28-9d2cc760558d` | 修复后通过 | 证明科学假设改动有效；随后同步新的远端主线 |
| `019f9d16-70d1-7b63-88ee-371db3cad93d` | 最新主线最终通过 | 作为改进后正式结果 |

前四条运行只用于定位、修复和同步前复核，不计入最终得分。

## 冻结量表评分

| 维度 | 得分 | 判据与证据 |
| --- | ---: | --- |
| 专家路由与闭合工具链 | 2/2 | 主 Agent 调用 `solar-hypothesis`；子 Agent 回执和正式产物共同证明六步合同闭合。 |
| 上游材料纪律 | 2/2 | 已完成但未通过 run 核验的复算、技术失败实验和未核验文献笔记均作为 `gap`；没有越界升级。 |
| 候选机制区分 | 2/2 | 发电机变化、平滑口径偏差、混合解释和随机涨落零假设在机制、预测与成对判别上可区分。 |
| 可证伪性 | 2/2 | 四个候选均有方向明确、可观测且会削弱自身的条件。 |
| 下一项检验区分力 | 2/2 | 多定义极小期长度对比明确给出三类结果分别支持或削弱哪个候选。 |
| 排序可追溯性 | 2/2 | 七维 rubric、全候选覆盖、连续名次、逐项理由完整；无已核验证据时如实使用空锚点。 |
| 不确定性与表述纪律 | 2/2 | 四个候选均为 `low`；证据缺口明确；无依据的月份贡献已被门禁删除。 |
| 产物完整性与一致性 | 2/2 | Markdown、组合 JSON、请求快照、run_id、内部哈希和文件哈希相互一致。 |
| **总分** | **16/16** | 达到冻结规程的全部要求。 |

## 正式产物

任务工作区：

`projects/default/runs/run_019f9d16-70cd-7460_dcf14bfa/hypothesis/runs/experiment_agent_handoff_demo-20260726T062812Z-26d4b2d0/`

| 文件 | SHA-256 |
| --- | --- |
| `hypotheses.md` | `56d295066079b199f5f394a04b299bd6ce817bcef886a7d93251425d1f951865` |
| `hypothesis_portfolio.json` | `4c65fe402f14fdeaaaa5b4b867fcc90d98df5e5c15959c5c12a41c2a341b46ee` |
| `hypothesis_request.json` | `6d61275c6d2618ae6d48d562f6c8f43e767bca88e40f8684068ed08626192956` |

复核结果：

- 请求文件的规范化哈希与组合中的 `request_sha256` 一致；
- 删除 `portfolio_sha256` 字段后重算的组合哈希与存档值一致；
- 组合状态为 `frozen`，候选数 4、排序条目数 4、证据登记数 3；
- 将正式组合重构为响应后重新执行语义检查，错误列表为空；
- `hypotheses.md` 的读者可见文字审校有一条“此前极少或没有”的修订痕迹提示；
  复核上下文后确认它描述历史活动周分布的待检验信号，不是版本或会话修订痕迹。

运行目录被 Git 忽略；仓库只提交本评测记录，不提交会话缓存、运行副本或凭据。

## 验证汇总

| 证据层 | 结果 |
| --- | --- |
| 科学假设独立确定性测试 | `72 passed` |
| 相关桥接与提示测试 | `37 passed` |
| 全仓后端测试 | `2846 passed, 16 skipped, 4 warnings` |
| 前端 lint | 0 errors；9 条既有 Fast Refresh warnings |
| 前端生产构建 | 编译、TypeScript、15/15 静态页面和 standalone 组装成功 |
| 静态检查 | Ruff F 规则、锁文件检查和 `git diff --check` 通过 |
| 真实模型运行 | `deepseek-v4-pro`，最终 run `success`，正式产物冻结成功 |
| 科学结论 | 仅形成待检验竞争假设；未验证发电机或口径解释何者为真 |
